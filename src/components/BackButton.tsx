import { Button } from "@mui/material"
import { grey } from "@mui/material/colors"

const BackButton = () => {
  return (
    <Button sx={{backgroundColor:grey[500],color:'#ffffff'}}>戻る</Button>
  )
}

export default BackButton